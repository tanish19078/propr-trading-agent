#include "propr/account/challenge_rules.h"

// Currently header-only. This .cpp is here so the build picks up the module under
// the glob, and to give us a stable TU if we later add validation routines.

namespace propr::account {}  // namespace propr::account
